// Exercise 4
#include <iostream>
using namespace std;
int main()
{
    int a = 2, b = 2, c;
    if (c = (a-b))
        cout << "The value of c is: " << c;
    // inside of if is zero, that's why program isn't executed
    return 0;
}